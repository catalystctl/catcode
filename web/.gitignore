# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# esbuild output of the custom server (generated by release-web.sh, transient)
/.server-bundle.js

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# env
.env
.env.*
!.env.example
.env*.local

# generated at build / install time (git commit of the running web)
/version.json

# local mobile audit screenshots / reports
.mobile-audit/

# catalyst-code core debug log
debug.jsonl

# ad-hoc UI test scripts (local dev only, not wired into package.json)
ui-test*.cjs
