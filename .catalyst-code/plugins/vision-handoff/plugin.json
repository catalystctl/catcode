{
  "name": "vision-handoff",
  "version": "0.1.0",
  "description": "Hand off image-bearing turns to the cheapest same-provider vision model when the active model lacks vision (recommended ON). Pin with vision_model or VISION_MODEL.",
  "hooks": {
    "pre_turn": {
      "script": "hooks/pre_turn.py",
      "timeout_ms": 5000,
      "pass_args": true
    }
  }
}
